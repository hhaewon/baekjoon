#include <stdio.h>

int main()
{
    int A[100];
    int N, M;
    int B[100];
    int dp[101][101] = {0};
    scanf("%d", &N);
    for (int i = 0; i < N; i++)
    {
        scanf("%d", &A[i]);
    }
    scanf("%d", &M);
    for (int i = 0; i < M; i++)
    {
        scanf("%d", &B[i]);
    }
    for (int i = 1; i <= N; i++)
    {
        for (int j = 1; j <= M; j++)
        {
        }
    }
}