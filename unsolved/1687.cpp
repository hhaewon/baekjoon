#include <iostream>

using namespace std;

int arr[333][333];
int sum[333 * 333];
int N, M;

int covertxy(int x, int y)
{
    return M * x + y;
}

int main()
{
    cin >> N >> M;
    for (int i = 0; i < N; i++)
    {
        string temp;
        cin >> temp;
        for (int j = 0; j < M; j++)
        {
            arr[i][j] = temp[j];
            sums[cnt] = sums[]
        }
    }
}