#include <iostream>
using namespace std;

typedef long long ll;

bool isNearPrime[100000000000001];

int main()
{
    cin.tie(NULL);
    ios::sync_with_stdio(false);

    ll A, B;
    cin >> A >> B;
    isNearPrime[1] = false;

    for (int i = 2; i<=B;i++) {
        if (isNearPrime)
    }
}